import { DateTimeGeneratePipe } from './date-time-generate.pipe';

describe('DateTimeGeneratePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeGeneratePipe();
    expect(pipe).toBeTruthy();
  });
});
